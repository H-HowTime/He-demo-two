<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atguigu.guli.service.cms.mapper.AdMapper">

    <select id="getAdPage" resultType="com.atguigu.guli.service.cms.entity.vo.AdVo">
        select ad.id, ad.title,ad.sort,ad.type_id,adt.title type
        from cms_ad ad
        left join cms_ad_type adt
        on ad.type_id = adt.id
        ${ew.customSqlSegment}
    </select>

</mapper>
